import { StyleSheet, Text, View } from "react-native";

export default function NewPostScreen({ navigation }) {
	return (
		<View>
			<Text>New post section here</Text>
		</View>
	);
}

const styles = StyleSheet.create({});
